import {h, Component} from 'preact';

export default class Header extends Component {
    /**
     * Preact render function
     *
     * @returns {*}
     */
    render() {
        return (
            <div>
                Header
            </div>
        );
    }
}
